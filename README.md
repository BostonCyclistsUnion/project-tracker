# project-tracker

Track Boston area micromobility projects

## Development

To test changes locally, you can use [Python](https://www.python.org/) to run a very simple webserver.

```bash
python -m http.server 8000
```

Then navigate to http://localhost:8000 in your web browser
